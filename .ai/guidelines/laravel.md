## Eloquent

- Always create `$fillable` property in Eloquent models.

## Type Declarations

- Always include `declare(strict_types=1);` at the top of every PHP file.
- Include PHPDoc blocks for complex return types and method parameters.
