$items = \App\Models\Menu::whereNull('parent_id')->orderBy('order')->get(['id','title','order']);
foreach($items as $item) { echo $item->id . ' | ' . $item->title . ' | order:' . $item->order . "\n"; }
